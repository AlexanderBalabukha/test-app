import { FilterCrudPipe } from './filter-crud.pipe';

describe('FilterCrudPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCrudPipe();
    expect(pipe).toBeTruthy();
  });
});
